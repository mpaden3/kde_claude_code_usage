# Rust build artifacts
/collector/target/

# The collector binary is built and bundled into the package at install time;
# never commit the compiled binary.
/package/contents/code/claude-usage-collector

# Editor / OS cruft
.idea/
